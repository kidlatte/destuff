<MudLink Href="@($"/locations/{Model.Slug}")"
         UserAttributes="@(new Dictionary<string, object> { { "title", @Model.PathString ?? "" } })">
    @Model.Name
</MudLink>

@code {
    [Parameter]
    public required LocationListItem Model { get; set; }
}
